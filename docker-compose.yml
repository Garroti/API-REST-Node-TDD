version: "3"

services:
  app:
    build: .
    command: npm start
    ports: 
      - "3001:3001"
    volumes:
      - .:/usr/app
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: barriga
    volumes: 
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"